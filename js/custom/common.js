//djConfig.noLoadirect=true;
    //dojo.loadAllRequireJs();

// this is for temporary use only
var __ = function(str) {
    return str;
}

function varDump(obj) {
	var out = '';
	for (var i in obj) {
		out += i + ": " + obj[i] + "\n";
	}
	alert(out);
}

